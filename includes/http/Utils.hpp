#ifndef UTILS_HPP
# define UTILS_HPP

#include <string>
#include <iostream>
#include <sstream>


std::string	toString(size_t val);
void        trim(std::string &line);
std::string toLower(std::string str);

#endif